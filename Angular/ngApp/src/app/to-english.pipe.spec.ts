import { ToEnglishPipe } from './to-english.pipe';

describe('ToEnglishPipe', () => {
  it('create an instance', () => {
    const pipe = new ToEnglishPipe();
    expect(pipe).toBeTruthy();
  });
});
